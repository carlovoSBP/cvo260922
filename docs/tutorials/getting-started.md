# Getting started with Paleofuturistic Python Project

<!-- coaching: This is a TUTORIAL page (Diátaxis). Its job is to take a newcomer by the hand through
     their first successful experience with your software — a guaranteed small win, end to end.
     Tutorials teach by doing: concrete steps, no options, no theory. Keep explanations for the
     Explanation section and lookup detail for Reference. Delete these coaching comments as you fill
     each section in. See ../developer/how-to/document-your-project.md for the full authoring guide. -->

In this tutorial you'll install Paleofuturistic Python Project and run it end to end for the first time. By the end you'll have _describe the small, concrete thing the reader will have achieved_.

## You need

<!-- coaching: List the true prerequisites only — what must exist before step 1 works. A Python
     version, credentials, a running service. Fewer is better. -->

- Python (see the supported versions on the [PyPI page](https://pypi.org/project/cvo260922/)).

## Step 1 — Install

```bash
uv add cvo260922
```

or, with pip: `pip install cvo260922`.

## Step 2 — The smallest possible run

<!-- coaching: Show the smallest complete use of your software that produces a visible result —
     a few lines of code or one command, followed by the exact output the reader should see.
     Resist adding options or alternatives; a tutorial has exactly one path. -->

```python
import cvo260922

# _Replace with the smallest real call into your package and show its output below._
```

You should see:

```
_the expected output_
```

## Step 3 — What you just did

<!-- coaching: One short paragraph connecting the steps to the concepts: "you created X, which did Y".
     Then hand the reader onward. -->

_Explain in two sentences what happened, then link the natural next step._

## Next steps

- _Link your first how-to guide here once it exists._
- [About Paleofuturistic Python Project](../explanation/about-your-project.md) — the why behind the project.
